--- LVM2.2.02.132/libdaemon/server/daemon-server.c~	2015-09-22 11:00:58.000000000 -0400
+++ LVM2.2.02.132/libdaemon/server/daemon-server.c	2015-11-14 19:25:14.924917337 -0500
@@ -30,6 +30,7 @@
 #include <sys/un.h>
 #include <unistd.h>
 #include <signal.h>
+#include <malloc.h>
 
 #include <syslog.h> /* FIXME. For the global closelog(). */
 
@@ -624,6 +624,7 @@
 
 	while (!_shutdown_requested && !failed) {
 		_reset_timeout(s);
+		malloc_trim(0);
 		FD_ZERO(&in);
 		FD_SET(s.socket_fd, &in);
 		if (select(FD_SETSIZE, &in, NULL, NULL, _get_timeout(s)) < 0 && errno != EINTR)
